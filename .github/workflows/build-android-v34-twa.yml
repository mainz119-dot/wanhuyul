name: Build 우리집 Android APK v34 - TWA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:

      # --------------------------------------------------
      # 1. GitHub 저장소 가져오기
      # --------------------------------------------------
      - name: Checkout
        uses: actions/checkout@v4


      # --------------------------------------------------
      # 2. Java
      # --------------------------------------------------
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"


      # --------------------------------------------------
      # 3. Android SDK
      # --------------------------------------------------
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3


      # --------------------------------------------------
      # 4. Gradle
      # --------------------------------------------------
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"


      # --------------------------------------------------
      # 5. TWA Android 프로젝트 생성
      # --------------------------------------------------
      - name: Create TWA project
        shell: bash
        run: |

          set -euo pipefail

          echo "Creating TWA project..."

          mkdir -p twa/app/src/main/java/com/woorijip/family
          mkdir -p twa/app/src/main/res/values


          # ------------------------------------------------
          # settings.gradle
          # ------------------------------------------------

          cat > twa/settings.gradle <<'EOF'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }

          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

              repositories {
                  google()
                  mavenCentral()
              }
          }

          rootProject.name = "WoorijipTWA"

          include(":app")
          EOF


          # ------------------------------------------------
          # build.gradle
          # ------------------------------------------------

          cat > twa/build.gradle <<'EOF'
          plugins {
              id "com.android.application" version "8.6.1" apply false
          }
          EOF


          # ------------------------------------------------
          # ★ gradle.properties
          # ------------------------------------------------
          # 이번 오류의 핵심 수정 부분
          # ------------------------------------------------

          cat > twa/gradle.properties <<'EOF'
          android.useAndroidX=true
          android.enableJetifier=true
          org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
          EOF


          # ------------------------------------------------
          # app/build.gradle
          # ------------------------------------------------

          cat > twa/app/build.gradle <<'EOF'
          plugins {
              id "com.android.application"
          }

          android {

              namespace "com.woorijip.family"

              compileSdk 35

              defaultConfig {

                  applicationId "com.woorijip.family"

                  minSdk 23

                  targetSdk 35

                  versionCode 34

                  versionName "34.0"

              }

              buildTypes {

                  release {

                      minifyEnabled false

                  }

              }

          }

          dependencies {

              implementation "androidx.browser:browser:1.9.0"

          }
          EOF


          # ------------------------------------------------
          # strings.xml
          # ------------------------------------------------

          cat > twa/app/src/main/res/values/strings.xml <<'EOF'
          <resources>

              <string name="app_name">우리집</string>

          </resources>
          EOF


          # ------------------------------------------------
          # styles.xml
          # ------------------------------------------------

          cat > twa/app/src/main/res/values/styles.xml <<'EOF'
          <resources>

              <style
                  name="AppTheme"
                  parent="android:style/Theme.Material.Light.NoActionBar">

                  <item name="android:windowNoTitle">true</item>

              </style>

          </resources>
          EOF


          # ------------------------------------------------
          # AndroidManifest.xml
          # ------------------------------------------------

          cat > twa/app/src/main/AndroidManifest.xml <<'EOF'
          <manifest
              xmlns:android="http://schemas.android.com/apk/res/android">

              <uses-permission
                  android:name="android.permission.INTERNET" />

              <application

                  android:allowBackup="false"

                  android:label="@string/app_name"

                  android:supportsRtl="true"

                  android:theme="@style/AppTheme">

                  <activity

                      android:name=".MainActivity"

                      android:exported="true">

                      <intent-filter>

                          <action
                              android:name="android.intent.action.MAIN" />

                          <category
                              android:name="android.intent.category.LAUNCHER" />

                      </intent-filter>

                  </activity>

              </application>

          </manifest>
          EOF


          # ------------------------------------------------
          # MainActivity.java
          # ------------------------------------------------

          cat > twa/app/src/main/java/com/woorijip/family/MainActivity.java <<'EOF'
          package com.woorijip.family;

          import android.app.Activity;
          import android.os.Bundle;
          import android.net.Uri;

          import androidx.browser.trusted.TrustedWebActivityIntentBuilder;

          public class MainActivity extends Activity {

              private static final String START_URL =
                      "https://mainz119-dot.github.io/wanhuyul/";

              @Override
              protected void onCreate(Bundle savedInstanceState) {

                  super.onCreate(savedInstanceState);

                  Uri uri =
                          Uri.parse(START_URL);

                  TrustedWebActivityIntentBuilder builder =
                          new TrustedWebActivityIntentBuilder(uri);

                  builder
                          .build(this)
                          .launch();

                  finish();

              }

          }
          EOF


          echo "TWA project created."


      # --------------------------------------------------
      # 6. APK 빌드
      # --------------------------------------------------

      - name: Build APK
        working-directory: twa
        run: |

          gradle assembleRelease --no-daemon


      # --------------------------------------------------
      # 7. 테스트용 서명키 생성
      # --------------------------------------------------

      - name: Generate test signing key
        shell: bash
        run: |

          set -euo pipefail

          mkdir -p output

          keytool -genkeypair \
            -keystore output/woorijip-v34-test.jks \
            -storepass android \
            -keypass android \
            -alias woorijip \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=우리집 TWA, OU=Family, O=Woorijip, C=KR"


      # --------------------------------------------------
      # 8. 인증서 SHA-256 확인
      # --------------------------------------------------

      - name: Get SHA256 fingerprint
        shell: bash
        run: |

          set -euo pipefail

          keytool -list -v \
            -keystore output/woorijip-v34-test.jks \
            -storepass android \
            > output/certificate.txt

          echo "======================================"

          grep "SHA256:" output/certificate.txt || true

          echo "======================================"


      # --------------------------------------------------
      # 9. APK 서명
      # --------------------------------------------------

      - name: Sign APK
        shell: bash
        run: |

          set -euo pipefail

          $ANDROID_HOME/build-tools/35.0.0/apksigner sign \
            --ks output/woorijip-v34-test.jks \
            --ks-pass pass:android \
            --key-pass pass:android \
            --out output/우리집-v34-twa.apk \
            twa/app/build/outputs/apk/release/app-release-unsigned.apk


      # --------------------------------------------------
      # 10. APK 검증
      # --------------------------------------------------

      - name: Verify APK
        shell: bash
        run: |

          $ANDROID_HOME/build-tools/35.0.0/apksigner verify \
            --verbose \
            output/우리집-v34-twa.apk


      # --------------------------------------------------
      # 11. APK 결과 업로드
      # --------------------------------------------------

      - name: Upload APK
        uses: actions/upload-artifact@v4

        with:

          name: woorijip-v34-twa

          path: |

            output/우리집-v34-twa.apk

            output/certificate.txt
